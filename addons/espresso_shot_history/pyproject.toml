[project]
name = "espresso-shot-history"
version = "1.0.0"
description = "Home Assistant add-on: Espresso shot history tracker"
requires-python = ">=3.12"
dependencies = [
    "aiohttp==3.13.3",
    "websockets==12.0",
]

[tool.uv]
package = false
